# PostHog (client-side ingest). The project API key is write-only and safe to
# ship to the browser. Points at the local hobby stack by default.
NEXT_PUBLIC_POSTHOG_KEY=phc_tChSZcLR2SMfb6CvaCECzHrPBgjKGjJ4UycREG8YiVGL
NEXT_PUBLIC_POSTHOG_HOST=http://localhost:8000

# Stripe (TEST MODE). Secret key is server-only — used to create Checkout
# Sessions in lib/stripe.ts. Get it from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_xxx
# Optional: only needed if you add client-side Stripe.js later.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
